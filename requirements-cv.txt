# CV analyzer — local-only pipeline (no cloud APIs)
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
scikit-image>=0.21.0

# Classifier (optional — only needed for train/label/--model)
torch>=2.1.0
torchvision>=0.16.0

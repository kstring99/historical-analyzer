# CV analyzer — local-only pipeline (no cloud APIs)
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
scikit-image>=0.21.0

# Web app
fastapi>=0.100.0
uvicorn>=0.20.0
python-multipart>=0.0.6

# PDF extraction
pdf2image>=1.16.0
pdfplumber>=0.10.0

# Classifier (optional — only needed for train/label/--model)
torch>=2.1.0
torchvision>=0.16.0
